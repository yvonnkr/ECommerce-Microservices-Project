spring:
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: gateway-service

#logging:
#  level:
#    org:
#      springframework:
#        cloud:
#          gateway: DEBUG

